import * as React from 'react';
import { BasePage } from '../../components/BasePage';

const List = () => {
    return (
        <BasePage title='Listagem de categoria' />
    );
};

export default List;